<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <%- include('./partials/head.ejs')%>
    </head>
    <body>
        
        <div class="main-container">
            <div class="main-col">
                <h2>
                    <% if(currentUser) { %> Welcome, <%=currentUser.username%>!
                    <br />
                    <h2>
                        <a class="dashboard" href="/app">
                            <button> Dashboard</button>
                        </a>
                    </h2>
                    <form action="/sessions?_method=DELETE" method="POST">
                        <input type="submit" value="Logout" id="logout-btn" />
                    </form>
                </h2>
                <% } else { %>
                <h1>Tired of not sticking to your workout plans?</h1>
                <div class="articles">
                    <article>
                        Fitness Tracker is an app to track all your workout plans
                    </article>
                    <br />
                    <article>
                        Achieve your dream body today
                       
                    </article>
                    <br />
                </div>
            </div>
            <div class="main-col">
                <h3>Welcome Back</h3>
                <a href="/sessions/new">
                    <button id="login">Log In</button>
                </a>
                <br />
                <h3>Register now</h3>
                <a href="/users/new">
                    <button id="signup">Register now</button>
                </a>
                <% } %>
            </div>
        </div>
    </body>
</html>